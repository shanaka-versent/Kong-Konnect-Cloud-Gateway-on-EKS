# ArgoCD App: Tenant App 1
# @author Shanaka Jayasundera - shanakaj@gmail.com
#
# Deploys sample-app-1 — route /app1 configured in Konnect.
# Exposed via internal NLB for Kong Cloud Gateway access.

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: tenant-app1
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "2"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default

  source:
    repoURL: https://github.com/shanaka-versent/Kong-Konnect-Cloud-Gateway-on-EKS.git
    targetRevision: HEAD
    path: k8s/apps/tenant-app1

  destination:
    server: https://kubernetes.default.svc
    namespace: tenant-app1

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
