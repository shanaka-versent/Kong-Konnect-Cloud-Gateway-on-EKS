# Kong Cloud Gateway on EKS - Users API Service
# @author Shanaka Jayasundera - shanakaj@gmail.com
#
# Sample REST API service for demonstrating Kong API management.
# Route (/api/users) and plugins (JWT, rate-limiting, CORS) are
# configured in Konnect -- see deck/kong.yaml for declarative config.
#
# Traffic flow:
#   Client --> Kong Cloud GW (JWT, rate-limit, CORS) --> Transit GW -->
#   Internal NLB --> Istio Gateway --> HTTPRoute --> users-api pod

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: users-api-config
  namespace: api-services
data:
  nginx.conf: |
    server {
        listen 8080;
        server_name _;

        location /api/users {
            default_type application/json;
            alias /usr/share/nginx/html/users.json;
        }

        location /health {
            return 200 '{"status": "healthy"}';
            default_type application/json;
        }

        location / {
            return 404 '{"error": "not found"}';
            default_type application/json;
        }
    }
  users.json: |
    {
      "users": [
        {
          "id": 1,
          "name": "Alice Smith",
          "email": "alice@example.com",
          "role": "admin"
        },
        {
          "id": 2,
          "name": "Bob Johnson",
          "email": "bob@example.com",
          "role": "user"
        },
        {
          "id": 3,
          "name": "Carol Williams",
          "email": "carol@example.com",
          "role": "user"
        }
      ],
      "metadata": {
        "version": "v1",
        "backend": "eks-kong-cloud-gw-istio",
        "timestamp": "2025-01-15T00:00:00Z"
      }
    }

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: users-api
  namespace: api-services
  labels:
    app: users-api
    version: v1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: users-api
  template:
    metadata:
      labels:
        app: users-api
        version: v1
    spec:
      containers:
        - name: users-api
          image: nginx:1.27-alpine
          ports:
            - containerPort: 8080
              name: http
          resources:
            requests:
              cpu: 10m
              memory: 16Mi
            limits:
              cpu: 100m
              memory: 64Mi
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 15
          volumeMounts:
            - name: config
              mountPath: /etc/nginx/conf.d/default.conf
              subPath: nginx.conf
            - name: config
              mountPath: /usr/share/nginx/html/users.json
              subPath: users.json
      volumes:
        - name: config
          configMap:
            name: users-api-config

---
apiVersion: v1
kind: Service
metadata:
  name: users-api
  namespace: api-services
  labels:
    app: users-api
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: users-api

---
# HTTPRoute for API traffic through Istio Gateway
# Kong Cloud Gateway sends /api/users traffic to the single NLB,
# Istio Gateway routes it to users-api pods via this HTTPRoute
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: users-api-route
  namespace: api-services
spec:
  parentRefs:
    - name: kong-cloud-gw-gateway
      namespace: istio-ingress
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /api/users
      backendRefs:
        - name: users-api
          port: 8080

---
# ReferenceGrant - allow HTTPRoute to reference services in api-services namespace
apiVersion: gateway.networking.k8s.io/v1beta1
kind: ReferenceGrant
metadata:
  name: allow-api-services
  namespace: api-services
spec:
  from:
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      namespace: api-services
  to:
    - group: ""
      kind: Service
